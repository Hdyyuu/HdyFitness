<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.example.hdy.dao.VideoMapper">
    <update id="updateVideoLikeCount">
        update video set likeCount = likeCount+#{1} where id = #{0,jdbcType=INTEGER}
    </update>
    <select id="selectByPrimaryKey" resultType="com.example.hdy.domain.Video" parameterType="java.lang.Integer" >
        <!--
          WARNING - @mbggenerated
          This element is automatically generated by MyBatis Generator, do not modify.
        -->
        select
        *
        from video
        where id = #{id,jdbcType=INTEGER}
    </select>
    <select id="selectAll" resultType="com.example.hdy.domain.Video" >
        <!--
          WARNING - @mbggenerated
          This element is automatically generated by MyBatis Generator, do not modify.
        -->
        select
        *
        from video
    </select>
    <select id="selectByType" resultType="com.example.hdy.domain.Video">
        select
            *
        from video where type=#{type, jdbcType=VARCHAR}
    </select>
    <select id="searchVideo" resultType="com.example.hdy.domain.Video">
        SELECT * FROM video WHERE title LIKE "%"#{keywords}"%" OR NAME LIKE "%"#{keywords}"%" or type like "%"#{keywords}"%"
    </select>
    <select id="selectByName" resultType="com.example.hdy.domain.Video">
        select * from video where name=#{name, jdbcType=VARCHAR}
    </select>
</mapper>
