<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.example.hdy.dao.RankMapper" >
  <delete id="deleteRank">
    delete  from `rank` where userID = #{userID, jdbcType=INTEGER} and videoID = #{videoID, jdbcType=INTEGER}
  </delete>
  <select id="selectScoreSum" resultType="java.lang.Integer">
  SELECT COALESCE(sum(score),0) as score from `rank` where videoID = #{0} ;
  </select>
  <select id="selectRankNum" resultType="java.lang.Integer">
  SELECT COALESCE(count(id),0) as num from `rank` where videoID = #{0} ;
  </select>
  <select id="isRanked" resultType="java.lang.Integer">
    select count(*) from `rank` where userID = #{userID, jdbcType=INTEGER} and videoID = #{videoID, jdbcType=INTEGER};
  </select>
  <select id="selectOneScore" resultType="java.lang.Integer">
    select score from `rank` where userID = #{userID, jdbcType=INTEGER} and videoID = #{videoID, jdbcType=INTEGER}
  </select>
  <insert id="insert" parameterType="com.example.hdy.domain.Rank" >
    <!--
      WARNING - @mbggenerated
      This element is automatically generated by MyBatis Generator, do not modify.
    -->
    insert into `rank` (userID,videoID,score)
    values (#{userID}, #{videoID},#{score})
  </insert>
  <insert id="insertSelective" parameterType="com.example.hdy.domain.Rank"  keyProperty="id" useGeneratedKeys="true" >
    <!--
      WARNING - @mbggenerated
      This element is automatically generated by MyBatis Generator, do not modify.
    -->
    insert into `rank`
    <trim prefix="(" suffix=")" suffixOverrides="," >

      <if test="userID != null" >
        userID,
      </if>
      <if test="videoID != null" >
        videoID,
      </if>
      <if test="score != null" >
        score,
      </if>
    </trim>
    <trim prefix="values (" suffix=")" suffixOverrides="," >
      <if test="userID != null" >
        #{userID,jdbcType=INTEGER},
      </if>
      <if test="videoID != null" >
        #{videoID,jdbcType=INTEGER},
      </if>
      <if test="score != null" >
        #{score,jdbcType=INTEGER},
      </if>
    </trim>
  </insert>
</mapper>
